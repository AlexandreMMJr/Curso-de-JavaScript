const raio = 5.6;
const area = Math.PI * Math.pow(raio, 2);

//Acima usando exemplos do 'Math", com 'PI' e 'pow' de elevado
console.log(area.toFixed(2));
console.log(typeof Math); //Retorna 'object'